# Application settings are stored in PostgreSQL. Compose needs only the
# external bootstrap values required to read them.
DATABASE_URL=Host=postgres;Port=5432;Database=agentplayground;Username=agentplayground;Password=agentplayground
CONFIG_ENCRYPTION_KEY=replace-with-a-base64-encoded-32-byte-key
